# Copy this file to .env and paste the class art key (your teacher posts it).
# .env is gitignored — it stays on your machine.
# Optional: without a key, the gallery still works and assets use placeholder art.
ART_API_KEY=
